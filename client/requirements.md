## Packages
framer-motion | For smooth page transitions and card animations
lucide-react | For beautiful UI icons
wouter | For routing (already in base, but good to note)
zod | For schema validation (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
